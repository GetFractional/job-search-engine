interface:
  display_name: "Build From Packet"
  short_description: "Implements one active packet with minimal context"
  default_prompt: "Use $build-from-packet to implement this active packet using only the project profile, the packet, and touched code."

policy:
  allow_implicit_invocation: false
