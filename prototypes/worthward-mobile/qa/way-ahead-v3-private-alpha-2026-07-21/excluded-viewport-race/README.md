# Rejected Evidence

These captures were taken before the requested viewport fully settled. Several have dimensions that do not match their filenames; others share the same capture loop and are conservatively excluded. They are retained only for auditability and must not support a responsive or completion claim.

Use the sibling `*-recheck.png`, `*-stable.png`, and explicitly listed evidence files in the parent register instead.
